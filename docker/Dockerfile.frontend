# HostWise Frontend Dockerfile — uses Bun for speed
FROM oven/bun:1-alpine

WORKDIR /app

# Install dependencies first (layer caching)
COPY package.json ./
RUN bun install --frozen-lockfile || bun install

# Application
COPY . .

EXPOSE 3000

CMD ["bun", "run", "dev"]
